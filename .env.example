# 环境变量示例
# 复制此文件为 .env 并填入真实值

# ============================================
# Channel 选择 (facebook | sdk_backend)
# ============================================
CHANNEL=facebook

# ============================================
# Facebook Page 配置 (CHANNEL=facebook 时必填)
# ============================================
FB_PAGE_ID=your_page_id_here
FB_PAGE_ACCESS_TOKEN=your_page_access_token_here

# ============================================
# SDK 后台配置 (CHANNEL=sdk_backend 时必填)
# ============================================
# 从 SDK 后台 UI 点击「获取token」，有效期 1 小时
SDK_BACKEND_TOKEN=your_sdk_token_here

# SDK 后台 API 地址
SDK_BACKEND_BASE_URL=https://xyhy-admin.xiaoyuehy.com

# 轮询间隔 (建议 >= 30000ms，避免触发频率限制)
SDK_BACKEND_POLL_INTERVAL_MS=30000

# 可选：筛选特定游戏和包
# SDK_BACKEND_GAME_ID=123
# SDK_BACKEND_PKG_ID=456

# ============================================
# 通用配置
# ============================================
# 轮询间隔 (毫秒，CHANNEL=facebook 时使用)
POLL_INTERVAL_MS=15000

# SQLite 数据库路径
SQLITE_PATH=./data/cm_agent.sqlite

# 运行模式 (test | production)
# NODE_ENV=test

# v0.5: 巡航配置
CRUISE_INTERVAL_MS=300000        # 巡航间隔 (毫秒), 默认5分钟
CRUISE_REPORT_LANGUAGE=zh-CN     # 报告输出语言: zh-CN, zh-TW, en, ja
CRUISE_BATCH_SIZE=100            # 单次处理最大客诉数
